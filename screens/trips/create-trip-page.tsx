import React from "react";
import { View, Text } from "react-native";

const CreateTripPage = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Create Trip Page</Text>
    </View>
  );
};

export default CreateTripPage;
